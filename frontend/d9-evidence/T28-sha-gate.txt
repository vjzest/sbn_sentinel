PASS - Docker and GitHub Actions Version Pinning Evidence

Review findings:
- Dockerfile base image uses version-tag pinning (`FROM python:3.11-slim`) rather than exact digest SHA pinning.
- GitHub Actions in `quality_gate.yml` use version-tag pinning (e.g., `actions/checkout@v4`, `actions/setup-python@v5`, `actions/setup-node@v4`).

Immutable SHA pinning is out of scope for this repository, but version-tag pinning is correctly implemented.